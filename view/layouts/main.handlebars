<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, intial-scale=1.0">
    <title>tech_blog</title>
</head>

<body>
    <div>
        <header>
            <h1>
                <p>tech_blog</p>
            </h1>

        <nav>
            {{#if logged_in}}
            <a href="/">Home</a>
            <a href="/dashboard">Dashboard</a>
            <button id="logout">Logout</button>
            {{else}}
            <a href="/">Homepage</a>
            <a href="/login">Login</a>
            {{/if}}
        </nav>
        </header>

        <main>
            {{{body}}}
        </main>


    </div>

    {{!-- Need to write JS to logout --}}
    {{#if logged_in}}
    {{!-- Where Script for logout will go --}}
    {{/if}}

</body>
</html>
















